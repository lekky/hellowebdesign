.DS_Store
Thumbs.db
node_modules/
*.log

# Claude Design handoff bundle (source material, not for deploy)
_design/
_design_index.html

# Local preview config (not for deploy). The UI/UX Pro Max skill bundle under
# .claude/skills/ IS committed so it is available in every session; the deploy
# workflow excludes .claude/** so it never reaches the web server.
.claude/*
!.claude/skills/

# Playwright MCP scratch artifacts
.playwright-mcp/

# Local dev/verification scratch (never deploy)
.dev/
